# Simple Java Base64 encoder/decoder, MIT (c) 2020-2021 miktim@mail.ru

package org.miktim

Overview:
  Class Base64 methods:
    static string encode(byte[] b); // encoded as single line
    static byte[] decode(String s); // decoder removes CRLF

See also core classes:
  - Java 8 and newer: java.util.Base64
  - Android API 8 and newer: android.util.Base64

